var mimicAlanGrant := func is
    print "I hate computers!"
end

var x := 0
loop
    print "Life always finds a way";
    x := x + 1;
    if x > 2 then
        exit
    else
        print "Not yet!"
    end
end

var y := 0
while y < 3 loop
        print "y is small!"
        y := y + 1
        if y = 2 then 
            print "y is not so small :("
            exit
        end
    end